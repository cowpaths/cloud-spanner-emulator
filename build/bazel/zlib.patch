diff --git a/zutil.h b/zutil.h
--- a/zutil.h
+++ b/zutil.h
@@ -23,7 +23,7 @@
 #  include <stdlib.h>
 #endif

-#if defined(MACOS) || defined(TARGET_OS_MAC)
+#if defined(MACOS) && !defined(__APPLE__)
 #  define fdopen(fd,mode) NULL /* No abs on abs - abs abs abs abs abs abs */
 #endif

